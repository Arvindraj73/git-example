git init - make folder a repo
git status - status of repo
git add . - add all files to staging area
git commit - commit the changes to repo
git commit -m "message"
git log --oneline - log in oneline
git chechout <commit> <file> - chechout file from older commit

to revert checked out file
git reset HEAD <filename>
git checkout -- <filename>


git reset - undo the add command

push an existing repository from the command line
git remote add origin https://github.com/Arvindraj73/git-example.git
git branch -M master
git push -u origin master
